function reverseChars(firstChar, secondChar, thirdChar) {
    let reversedChars = thirdChar + ' ' + secondChar + ' ' + firstChar;

    console.log(reversedChars);
}

reverseChars('A', 'B', 'C');
reverseChars('1', 'L', '&');